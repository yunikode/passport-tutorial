<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Passport</title>
</head>
<body>

  <% if( !isAuthenticated ) { %>
    <a href="/login">Log in here</a>
  <% } else { %>
    <h1>Hello, <%= user.name %>! </h1>
  <% } %>
</body>
</html>
